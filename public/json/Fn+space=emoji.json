{
 	"title": "Fn + space = emoji menu (command+control+space)",
   "rules": [
     {
 	"description": "Fn + space = emoji menu. (Simplifies default command + control + space)",
       "manipulators": [
         {
           "type": "basic",

           "from": {
             "key_code": "spacebar",
             "modifiers": {
               "mandatory": ["fn"]
             }
           },

           "to": [
             {
               "key_code": "spacebar",
 		"modifiers": ["left_command", "left_control"]
             }
           ]
         }
       ]
     }
   ]
 }
