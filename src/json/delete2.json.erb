{
    "title": "Change delete key (rev 2)",
    "rules": [
        {
            "description": "Change shift + delete to forward delete (rev 2)",
            "manipulators": [
                {
                    "type": "basic",
                    "from": <%= from("delete_or_backspace", ["left_command"], ["caps_lock", "option"]) %>,
                    "to": <%= to([["delete_forward"]]) %>
                }
            ]
        }
    ]
}
