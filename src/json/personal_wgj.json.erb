{
    "title": "Personal rules (@wgj)",
    "rules": [
        {
            "description": "Change insert to left_option+escape (VoiceOver)",
            "manipulators": [
                {
                    "type": "basic",
                    "from": <%= from("insert", [], ["any"]) %>,
                    "to": <%= to([["escape", ["left_option"]], ["vk_none"]]) %>
                }
            ]
        }
    ]
}