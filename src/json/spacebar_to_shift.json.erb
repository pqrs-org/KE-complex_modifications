{
    "title": "Change spacebar to left_shift (SandS)",
    "rules": [
        {
            "description": "Change spacebar to left_shift. (Post spacebar if pressed alone)",
            "manipulators": [
                {
                    "type": "basic",
                    "from": <%= from("spacebar", [], ["any"]) %>,
                    "to": <%= to([["left_shift"]]) %>,
                    "to_if_alone": <%= to([["spacebar"]]) %>
                }
            ]
        }
    ]
}
