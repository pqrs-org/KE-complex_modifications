# Usage

## Build container

```shell
docker-compose up
```

## Destroy container

```shell
docker-compose down
```

## Building for production

Run `make` command while the container is running.

```shell
make
```
